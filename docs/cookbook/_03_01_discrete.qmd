<!-- _03_01_discrete.qmd — Frequency: discrete -->

## Discrete frequency {#sec-frequency-discrete}

*This is a recipe for* deterministic and simple discrete counts: `fixed` (a known
number of claims), `dfreq [...]` (an explicit count distribution), and Bernoulli
(0/1) frequency.

```{python}
from _setup import *
```

**Beat 1 — the DecL.**

```{python}
pp('agg FixedCount 5 claims sev lognorm 50 cv 1.5 fixed')
```

**Beats 2 & 3 — build, validate, exhibits.**

<!-- TODO(author): fixed vs dfreq [1 2 3] with weights; show the count pmf. -->

**Beat 4 — the check.** [Check-Reconciliation]

<!-- TODO(author): with fixed frequency the aggregate is a pure n-fold
     convolution — mean = n x E[X] exactly. -->

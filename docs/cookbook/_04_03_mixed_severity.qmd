<!-- _04_03_mixed_severity.qmd — Aggregate: mixed severity -->

## Mixed severity {#sec-aggregate-mixed-severity}

*This is a recipe for* an aggregate whose severity is itself a mixture — an attritional component plus a large-loss component, each with its own distribution and weight. This is where the DecL earns its keep, and where the aggregate turns multi-modal.

```{python}
from _setup import *
```

<!-- TODO(author): a weighted two-component severity (e.g. attritional lognorm + large pareto) with weights, calibrated to the house scale. -->

<!-- TODO(author): show the component structure and the blended density. -->

::: {.callout-note collapse="true" title="The check"}
<!-- [Check-Reconciliation] -->

<!-- TODO(author): the mixture mean is the weighted mean of the components; the component weights recover from the spec. -->

:::

<!-- _06_01_consolidated.qmd — PnL consolidated -->

## Consolidated PnL {#sec-pnl-consolidated}

*This is a recipe for* the bottom line. A `pnl` wraps a complete engine and
presents **one consolidated net group**: consideration = net premium
(gross − ceded premiums + commissions), obligation = net loss + own expenses,
margin = the difference. The summary card reads in kappa (κ) scenario terms.

```{python}
from _setup import *
```

**Beat 1 — the DecL.**

```{python}
pp(pnl(house('Book', occ=OCC_LAYER)))
```

**Beats 2 & 3 — build, validate, the card.**

<!-- TODO(author): build; show summary_df (Consideration/Obligation/Margin) and
     construction_explanation. -->

**Beat 4 — the check.** [Check-Reconciliation]

<!-- TODO(author): net premium = gross − ceded premium + commission, to the penny
     (the economics constants). -->

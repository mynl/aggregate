---
wd-python:  T:/worktrees/aggregate_REFACTOR/.venv/Scripts/python.exe
---

<!-- this file provides setup so that the cookbook previews in writedown -->

Pin the quarto Python!

```bash
$env:QUARTO_PYTHON = "T:\worktrees\aggregate_REFACTOR\.venv\Scripts\python.exe"
```


```{python}
import sys
sys.path.append('T:\\worktrees\\aggregate_REFACTOR\\docs\\cookbook')

import warnings
warnings.filterwarnings("ignore", category=RuntimeWarning)

import aggregate.style
aggregate.style.use()
from aggregate.plots import make_grid
%config InlineBackend.figure_format = 'svg'
from aggregate.utilities import silence_warnings
from aggregate import __version__ as v, qd, build
# optional:
# silence_warnings()
print(v)
```